/**
 * @pebble/panels — the shared React component library the AI composes into panels.
 *
 * Skeleton: just PanelShell. This is the highest-leverage asset — it's reused
 * by BOTH the v1 "react" panel format AND the future "spec" format.
 */
export * from "./PanelShell";
